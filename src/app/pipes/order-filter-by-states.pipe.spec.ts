import { OrderFilterByStatesPipe } from './order-filter-by-states.pipe';

describe('OrderFilterByStatesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderFilterByStatesPipe();
    expect(pipe).toBeTruthy();
  });
});
