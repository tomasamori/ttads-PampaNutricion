import { OrderFilterByNumberPipe } from './order-filter-by-number.pipe';

describe('OrderFilterByNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderFilterByNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
