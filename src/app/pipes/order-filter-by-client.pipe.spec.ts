import { OrderFilterByClientPipe } from './order-filter-by-client.pipe';

describe('OrderFilterByClientPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderFilterByClientPipe();
    expect(pipe).toBeTruthy();
  });
});
